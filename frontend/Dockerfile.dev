# local source files mounted via Docker Compose
# Stage 1: Base environment
FROM node:20-alpine

# Set working directory inside the container
WORKDIR /app

# Copy only dependency files first (for caching)
COPY package*.json ./

# Install dependencies (once, cached unless package.json changes)
RUN npm ci

# The actual source code will come from your host machine at runtime via Docker volumes,
# so we don't copy src, public, or other files here.

# Expose the dev server port
EXPOSE 5173

# Default command to start Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]







